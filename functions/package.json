{
  "name": "functions",
  "description": "Alexandria cloud functions — resolveBook (official APIs + OpenRouter free models fallback) with Firestore cache",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "private": true
}
